__pycache__/
*.py[cod]
.env
.venv/
venv/
*.egg-info/
dist/
build/
.streamlit/secrets.toml
*.pdf
data/raw/*
!data/raw/.gitkeep
data/cache/*.json
data/embedding_cache/*.json
data/classification_cache/*.json
settings.local.json
nul
.mcp.json

# Local caches for thesis Google Doc roundtrips (see CLAUDE.md → Google Workspace References)
.thesis-*.json
.schema-batchupdate.json

# Playwright MCP session artifacts
.playwright-mcp/

# Neo4j Aura binary snapshots — pointers (.ref files) are in git instead.
# See experiments/knowledge_graph_states/*/snapshot.backup.ref
*.backup